<% @user ||= current_user %>
  <a href="<%= following_user_path(@user) %>">
    <strong id="following" class="statistics">
      <%= @user.following.count %>
    </strong>
    following
  </a>
  <a href="<%= followers_user_path(@user) %>">
    <strong id="followers" class="statistics">
      <%= @user.followers.count %>
    </strong>
    followers
  </a>